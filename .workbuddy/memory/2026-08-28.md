# 2026-08-28 工作日志

## MateLink/JourVolt 进度与缺口盘点（只读，未改代码/配置）

- 产出：`docs/PROGRESS-GAP-REVIEW-2026-08-28.md`（新增文档，未改任何源码）。
- on-disk 复核：分支 `codex/matelink-jurvolt-release-20260823`，HEAD `ec6c739`（8/23 12:46，已推 origin）；`git status --porcelain` 共 81 项未提交——8/23 之后 5 天的工作（珍珠电驱、阿里云 staging、MQTT 状态化、原创车型图、TPMS Task1 代码）全未入库。
- 门禁现状：Debug/Release JVM 各 322 项，0 failure/0 error；lintRelease 0 Error / 204 Warning / MissingTranslation=0 / 无 baseline。
- 外部门禁实测：`api.jourvolt.com`、`auth.jourvolt.com` 今日 nslookup 均 Non-existent domain（此前为 198.18.0.x fake-IP）；服务器采购预算门禁未满足（首年 ≤600 元 vs 阿里云 2C4G 约 1733.04 元/年）。域名/备案/Tesla 应用批准/正式 keystore/真实单车 Pilot 6 项全部未具备。
- TPMS 趋势与行程通知（计划 `tasks/plans/2026-08-28-tpms-trends-trip-notifications.md`）：todo 中 Task1–5 全未勾选；磁盘上 Task1 代码与测试文件已生成（19:24–19:51，疑似并行会话在写），Task2/3/4 目标文件均缺失，无测试通过证据。
- 记忆偏差：Obsidian `tesla-speed/tesla-master-app-mimo/02-当前进度.md` 最新只到 8/24；`tesla-speed/00-项目概览.md` 顶部权威状态仍停在 Stage J（8/11）。S12 主线 Stage W 卡在 REFERENCE_TARGET_MISSING（R1 intake 缺 sha256.txt / 原始 WAV/FLAC / 同步 RPM-load-gear）。

## 服务器部署与登录启用（用户三项决策）

用户已拍板：① 两条路径并行、自托管先跑通；② 域名与 Tesla 开发者应用**都已具备**（此前记忆记为 0/6，需更新）；③ 服务器用现有阿里云 ECS `120.55.64.11`。

### ECS 实测（只读 SSH，账户 `jourvolt`）

- 规格：**2 核 / 内存 1870 MB** / swap 2047 MB（已用 392）/ 磁盘 40G 剩 27G；Alibaba Cloud Linux 3，Docker 26.1.3。
- 已跑容器：`star-photo-star-weather-1` + worker（健康）、`jourvolt-staging-jourvolt-dev-api-1`（健康，绑 `127.0.0.1:18090`）、`jourvolt-staging-jourvolt-postgres-1`（postgres:16-alpine，健康）。
- `/healthz` = `{"mock_history":true,"mode":"mock_only","persistence":"postgres","status":"ok"}`。
- **硬约束 1：内存仅 1.87 GB**，TeslaMate 全套（Postgres+Elixir+Grafana+Mosquitto）叠加现有负载会 OOM，自托管路径必须裁剪（去掉 Grafana/Mosquitto 或复用已有实例）。
- **硬约束 2：`0.0.0.0:80` 已被宿主机进程占用**（非容器映射），443 未监听；Caddy edge 起不来，必须先定位占用进程再决定停服/反代/换端口。
- SSH 必用专用密钥：默认 id_ed25519 会 Permission denied，必须 `-i ~/.ssh/joviluma_jourvolt_deploy_ed25519`。

### 域名与 ECS 确认（2026-08-28 20:40）

- 域名：`joviluma.com`，DNS 在阿里云解析控制台。
- 已配置 A 记录：
  - `photo.joviluma.com` → 120.55.64.11（现有 star-photo 项目）
  - `teslalink.joviluma.com` → 120.55.64.11
  - `auth.teslalink.joviluma.com` → 120.55.64.11
  - `api.teslalink.joviluma.com` → 120.55.64.11
  - `@` 根域 → 120.55.64.11
- ECS 实例：`ecs.c1m1.large`（2 vCPU / 2 GiB / 1 Mbps 固定带宽 / 40G 系统盘），创建时间 2026-08-23，到期 2027-08-28，华东 1 杭州。
- 自托管数据：TeslaMate 原本跑在用户自己电脑的 Docker 上，**现在仍能运行**，可导出 `pg_dump` 后迁移到 ECS。
- 备案状态：服务器已备案，域名 `joviluma.com` **尚未做备案接入**；需要用户在阿里云备案系统提交新增网站/新增域名接入。

### 团队流程

以 software-team-lead 主理人身份建团队 `software-jourvolt-pilot`，顺序：PM 许清楚（PRD）→ 架构师 高见远 → 工程师 寇豆码 → QA 严过关。
- PM 任务已完成：`docs/PRD-2026-08-28-jourvolt-deploy-login.md`。
- 架构师任务两次启动均因平台 429 队列限流失败，已改用 SendMessage 唤醒继续。

### 备案与 T04 完成（22:30 前后）

- 备案通过：**苏ICP备2026062639号-1**，`joviluma.com` → `120.55.64.11`，网站名"生活记录本"。备案同步有约 7 小时延迟。
- sudo 配置完成：`/etc/sudoers.d/jourvolt`（NOPASSWD:ALL），实测 `sudo -n nginx -t` 通过。
- T04 完成（工程师二号，团队上下文曾遭平台重置、团队重建为 `software-jourvolt-pilot-7a5a`）：nginx 三域名 443 server 块上线、自签证书兜底、`verify-public.sh` PASS=19 FAIL=0；本机独立复测 capabilities 无 token 401、assetlinks 200、terms 200；`star-photo.conf` 只备份未改；`nginx -t` warning 维持基线 2 条（既有 `server_name _` 冲突）。9 个部署脚本 + 3 个 nginx 配置全部落盘。
- 证书签发待邮箱 → Jovi 提供 `977191131@qq.com`，已下达重跑 `setup-root.sh` 指令。

### 产品方向修正（重大，22:46）

- Jovi 澄清：App 将**分发给 7–8 个用户共同使用**；每人行车历史存各自手机 Room，服务器只存加密 token + 会话，不存行车历史。
- dump 迁移（T1②）**停止**——那是单人自用场景；个人历史 190 条留在手机。9 个迁移/自托管脚本保留但降级。
- TeslaMate 五容器已 `docker compose -f docker-compose.selfhost.yml stop`（**非 down，数据卷保留**），内存 available 回到 1178MB。恢复：`cd /home/jourvolt/matelink-selfhost && docker compose -f docker-compose.selfhost.yml start`（**必须带 -f**，该目录无默认 compose 文件名）。
- Tesla 应用：注册在 **tesla.com**，**未审核**，scope/回调未确认 → 未审核状态只有开发者本人账号能完成 Fleet 授权，其他 7 人被 Tesla 审核阻塞（非代码问题）。
- `/privacy/` `/terms/` = Tesla 第三方应用审核强制要求的隐私政策/用户协议页面；条款需写明"行车数据仅存用户手机，服务器只存加密凭据"。
- APK 分发：`https://auth.teslalink.joviluma.com/download/` 下载页任务已下达。签名沿用与手机原包一致的现有证书（统一即可升级），上架市场时再换官方 keystore。
- comet CLI 故障：`comet workflow resolve` 两次失败于 `[safe-delete] trash 操作被中止`（`.comet/.classic-init-ownership.*.tmp`），OpenSpec 结构已创建但工作流入口未激活；未绕过，待 comet-windows-setup 修复。

### 架构事实澄清（23:20–23:35，代码证实）

- Jovi 疑问："App 是不是不直连 Tesla，而是走 MateLink？"——**证实**：App 只有唯一 Retrofit 接口 `TeslamateApi`（`api/matelink/v1/*` + `api/v1/*`），从不直连 Tesla；`TeslamateRepository` 第 151 行按 `ConnectionMode` 只换 baseUrl（TESLA_CLOUD → `BuildConfig.JOURVOLT_API_BASE_URL`，SELF_HOSTED → 用户自填地址），请求/解析代码两模式完全一致。
- **重大缺口**：云端模式下历史为空。`main.go` 566–589 行：fleet 非 mock 时 drives/charges 返回空数组；App 侧历史唯一入口是 `SyncRepository.syncDriveSummaries/ChargeSummaries`（从后端同步），无本地自产机制。根因：Tesla 官方 API 无行程/充电历史端点，TeslaMate 的历史是它 7×24 轮询自攒的。
- 已给 Jovi 三个方案对比：TeslaMate 单套（仅 1 用户可用）/ 每人一套（内存 8×276MB≈2.2GB > 1870MB、配额被封、运维爆炸）/ **Fleet API 多租户 + App 手机本地攒历史（推荐，符合"数据在手机"）**。等 Jovi 拍板 A（本地攒）或 B（云端无历史）。
- Jovi 又问"为何不能 App→JourVolt→Adapter→TeslaMate 服务所有人"——已解释：TeslaMate 数据库无用户维度（多用户数据混锅）、每人一套装不下、高频轮询撞官方配额。

### 路线三拍板与文档化（23:56–00:15）

- **Jovi 已拍板路线三：Fleet API 多租户 + 手机本地历史（方案 A）**，并否决 TeslaMate 服务多人的两条路线。
- 落盘 `docs/ARCH-DECISION-2026-08-28-route3-fleet-multitenant.md`（权威决策文档，含三路线对比、代码证实的技术事实、阶段 0–3 执行计划、后续 agent 行为指令）。
- 同步 Obsidian 知识库：新建 `tesla-master-app-mimo/12-路线三-FleetAPI多租户与本地历史决策-2026-08-28.md`；`03-关键决策.md` 置顶新增第 0 条；`02-当前进度.md` 头部插入"2026-08-28 权威状态"块（此前该页最新只到 8/21）。
- 误诊纠正（记录以防复发）：① "客户端写的是 http://120.55.64.11"——代码里不存在该地址（Release 默认 `https://api.jourvolt.com/`，新 APK 由 build-pilot-apk.ps1 强制传 `https://api.teslalink.joviluma.com/` 且拒绝 HTTP/裸 IP）；② "nginx 的 18080 应改成 18090"——误诊，18080=自托管 Adapter（容器已 stop 故 refused）、18090=Go API，502 是停容器的预期结果，**已拒绝执行该修改**。
- 待办主线：阶段 1（fleet 就绪 + APK 上架，工程师三号进行中）→ 阶段 2（方案 A 本地历史新功能）→ 阶段 3（Jovi Pilot + Tesla 审核 + 7 人分发）。

### 阶段 1 由主理人亲自完成（8/29 00:35–00:50，工程师三号卡死 60+ 分钟后接管）

- **APK 已构建并签名成功**（本机 build-pilot-apk.ps1，8m28s）：`com.matelink` / `SIGNED_RELEASE` / `apksigner_verified` / versionName `1.4.2`(versionCode 14) / SHA-256 `02D84ED4034F3403458380F4132941AE601E1F2122AB270CB79ADC3E693B3E71` / 62,230,959 字节 / `android/app/build/outputs/apk/release/app-release.apk`。
- 签名配置：`E:/Claude_allow/matelink-release.properties`（仓库外，四键齐全，storeFile → `C:/Users/Admin/.android/debug.keystore`）。证书指纹 `9AB144E8...81FC774`（DN `CN=Android Debug`）**与 8/23 v49 一致 → 可覆盖安装用户手机现有包，Room 190 条历史不丢**。取舍：这是 Android 默认调试证书非发布 keystore；换正式 keystore 需卸载重装（丢手机本地历史），列为长期决策。
- 下载页 `https://auth.teslalink.joviluma.com/download/` 已上架真实 APK（含版本/大小/SHA-256/安装步骤），公网 200 且 Content-Length 一致；assetlinks.json 已写入真实证书指纹（32 段冒号格式），公网可读。
- **条款页按路线三重写**：原 8/21 版本仍写"行程摘要保存 1 年""删除云端车辆数据""加密备份"，与"数据仅存手机"矛盾。`web_matelink/public/privacy/index.html` 与 `terms/index.html` 已重写并部署，公网 200；运营主体/联系邮箱/备案号为占位符待 Jovi 填。
- **平台团队通道不稳定**：团队上下文两次被重置（SendMessage 报 Not in a team / No active team），工程师三号长时间无响应。结论：关键路径（构建、部署、配置）由主理人直接执行更可靠，agent 仅用于可并行的独立产出。
- 剩余阻塞：**仅 TESLA_CLIENT_ID/SECRET 需 Jovi 亲手填**（`docs/JOURVOLT-ECS-SECRET-SETUP.md`），填完 Go API 即可切 fleet；另需 Jovi 从 Tesla 控制台取 3p 公钥供 `--pubkey` 发布。
