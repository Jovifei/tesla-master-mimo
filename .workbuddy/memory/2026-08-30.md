# 2026-08-30

## 代码更新梳理（用户请求：最近更新了哪些功能）

- main 在 5f3fee1 之后新增 5 个提交（8-29 21:15 → 8-30 14:17），主题全部围绕 Tesla 中国授权链路 + TPMS 设置页：
  1. `9e06595` feat(ui)：TPMS 独立设置页（TpmsSettingsScreen，胎压目标/提醒阈值从设置主页拆出）+ 登录页法务链接升级全宽 OutlinedButton 并显示 URL；JVM 376/376；签名 APK 189AB5F4 真机覆盖安装成功
  2. `0584b66` fix(auth)：App 第一跳授权链接校验对齐服务端回调架构——redirect_uri 改为要求 `https://<API host>/v1/auth/tesla/callback`，ViewModel 改传 JOURVOLT_API_BASE_URL；JVM 376/376；APK 09EDDB88 真机安装成功
  3. `ef1353d` feat(api)：`/oauth/callback` 兜底页（applink_fallback.go）——国内 GMS App Link 验证失败（DEADLINE_EXCEEDED），页面自包含中文 HTML、立即 intent:// 拉起 com.matelink、sessionStorage 防循环、1.5s 后手动按钮；挂载在会话门禁前；6 单测 + go vet 绿；已部署 ECS 公网验证三分支
  4. `5bc6de1` fix(android)：登录成功后进仪表盘（navigateToDashboardAfterTeslaAuth，popUpTo start inclusive）+ 重复授权回调幂等（同 ticket 被 App Link 与手动返回重复投递不再误报失败）；新增 TeslaCallbackReplayGuardTest；CustomTabs 启动移至 NavGraph（pendingAuthorizationUrl）；服务端新增 tesla_oauth_error.go
  5. `ba08db8` fix(api)：按 ID Token 实际 iss 校验——Tesla 中国 RFC 9207 issuer 与 token iss 可分别 /oauth2/v3 与 /oauth2/v3/nts；新增 peekJWTIssuer（不验签解 payload 取 iss）+ 候选 verifier 依次尝试；verifyIDToken
- 推送状态：远端 main = 5bc6de1，本地 main = ba08db8，**本地 ahead 1 未推送**；codex 分支远端仍 5f3fee1。未推送（默认 no-push）。
- 工作区未提交改动（Task A–D「数据完整性与首次登录体验」，团队 software-jourvolt-pilot 遗留，任务边界明确"不提交、不推送、不部署"）：79 文件修改（+3779/-718）+ 58 个新文件
  - Task A（服务端，已完成）：位置/四轮胎压/数据就绪状态字段（DataReadinessModels.kt、readiness.go）
  - Task B（Android，已完成）：电量提示、电池健康错误、首次登录数据准备弹窗、永久数据状态入口（DataReadinessStore、ui/screens/readiness/）
  - Task C（Android，已完成+Review PASS）：稳定车辆上下文 VehicleContext（Store/Repository/DI module）、LegacyHistoryArchive（entity/DAO）、UnifiedHistoryRepository、LegacyHistoryMigrationRepository、Room schema v18/v19；fresh Debug JVM 434/434
  - Task D（Fleet Telemetry 服务，骨架完成、blockers 未完）：telemetry_config/core/mqtt/http/service/store + QoS1 重投/重启幂等（Postgres postgres:16-alpine 集成测试过）、compose 证书边界；go test/vet 绿
  - Task D blockers（7 项 RED/GREEN）+ Task E（总复核）均未完成
