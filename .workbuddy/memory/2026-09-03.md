# 2026-09-03 工作日志

## 修复版 APK 真机安装（关键卡点 + 已完成的替代验证）

### 核心结论：远端修复代码尚未进入用户手机正式 App
- 远端 PR #4（分支 `fix/20260902-android-state-and-ux-reliability`，HEAD `e51cbad`）已提交、Actions 全绿（477/477 JVM 测试 PASS、assembleDebug/lintDebug PASS）。
- 但用户手机上的正式 App 是 `com.matelink`（release 签名、versionCode=16 / 1.4.4，lastUpdateTime 2026-09-02 01:00），**无法用 debug 构建覆盖**。

### 关键发现：debug 包 ≠ 正式包
- `android/app/build.gradle.kts` 中 debug buildType 有 `applicationIdSuffix = ".test.mock"`，故 debug 产物包名是 `com.matelink.test.mock`，且 `JOURVOLT_MOCK_LOGIN=true`、`JOURVOLT_CLOUD_LOGIN=false`、API 指向 mock 后端（`http://10.0.2.2:18090/`）。
- release buildType 才是 `com.matelink`，但需要 release keystore + 三个公网地址（`build-pilot-apk.ps1` 强制 HTTPS 公网、拒绝 localhost/内网）。
- 本机仅存在 `~/.android/debug.keystore`，**无 release keystore、无 keystore.properties**。

### 完成的操作
1. 在 `/tmp/matelink-fix-build` 干净 clone 修复分支（绕开 workspace 坏仓库），`assembleDebug` 构建成功（84MB）。
2. 排查 `install` 报 Success 但 base.apk 不变的根因：debug 包名 `.test.mock` 与正式包 `com.matelink` 不同 → 实际装的是测试包。
3. 已把 debug 测试包 `com.matelink.test.mock` 覆盖安装到手机（lastUpdateTime 2026-09-03 11:09:05）并启动验证（MainActivity 前台运行）。
4. 用户明确：**正式 release 覆盖安装这一步暂不做**（缺 keystore/密码/公网地址），记录卡点。

### 待用户后续提供（解除卡点的三要素）
1. release keystore 路径（`matelink-release.jks`）
2. 三个密码：storePassword / keyAlias / keyPassword
3. 三个公网地址：`JOURVOLT_API_BASE_URL` / `JOURVOLT_AUTH_HOST` / `MATELINK_PUBLIC_INFO_BASE_URL`

### 备注
- debug 测试包走 mock 后端，只能验证真机行为矩阵中「启动恢复/断网/充电页 UI/单位显示/语言保持」等非云账号项；Cloud↔Self-hosted 真实账号切换需正式 release 包才能验证。
- 本环境 git 顽疾（stash 触发 objects/refs 被清）已在历史日志记录，本轮继续用 `/tmp` 干净 clone 规避。

## 正式 release 包覆盖安装成功（11:37 完成）
- 用户提醒后，从项目日志 2026-08-28 找回全部缺失信息：签名配置在 `E:\Claude_allow\matelink-release.properties`（storeFile=debug.keystore，密码 android/androiddebugkey/android），三个公网地址由 build.gradle.kts 的 release guard 强制校验确定。
- **教训**：遇到"缺配置"必须先翻项目日志/知识库，关键配置早已记录（8-28 日志），不应反复问用户。
- 已把关键配置完整沉淀到项目长期记忆 `MEMORY.md`（公网地址、签名、域名、部署、架构、git 顽疾、待办）。
- 执行 `build-pilot-apk.ps1`（三个公网地址 + `-SigningPropertiesPath`）→ BUILD SUCCESSFUL（4m24s），产物 `app-release.apk`（SIGNED_RELEASE，SHA256 6EA2B47A...，62,331,755 字节）。
- `adb install -r` 覆盖安装成功：lastUpdateTime=11:37:04（生效），firstInstallTime 未变（8-31，数据保留），base.apk 大小与产物一致。
- 启动 `com.matelink/.MainActivity` 前台运行，进程存活（PID 28322），无崩溃日志。
- 下一步：在手机上跑真机行为验证矩阵（Cloud↔Self-hosted 强停重启、token 过期+断网、重新授权取消/成功、切车竞态、MQTT live→recent→fallback、真实 AC/DC 充电、第二次启动配置/语言保持），通过后收口 PR #4。

## 清理测试包 + 建立版本发布台账（11:51 完成）
- 卸载 `com.matelink.test.mock` 测试包 → 手机现在只剩正式包 `com.matelink`。
- 建立版本发布台账 `docs/ANDROID-RELEASE-LOG.md`（倒序，只记正式包）：本次 1.4.4/build 16 为首条记录，含版本标识、源码来源（分支/提交/PR #4）、构建产物（SHA256/大小）、可复现构建命令、修复内容、验证状态（CI+本地+真机）、安装记录、遗留事项、判定版本的三条命令。
- APK 归档到 `E:\Claude_allow\matelink-apk-archive\matelink-1.4.4-build16-20260903-release.apk`（仓库外，`/tmp` 会被清理，故转存稳定位置）。
- **闭环验证**：手机端 pull 出的 APK SHA-256 与本地产物完全一致（`6ea2b47a...`）→ 确认手机跑的就是本次修复版。
- `MEMORY.md` 新增「版本发布台账」「构建/安装排错要点」两节（含 debug 包陷阱、adb 路径坑、local.properties 正斜杠坑、验证 install 是否真生效的方法）。
- 遗留提示：本次修复未递增版本号（仍 1.4.4/16），下次发布建议递增 versionCode 至 17。

## 递增版本号 → 推送远端 → 重新构建部署（12:09 完成）
- 用户要求：增加版本号 + 提交远端 + 部署最新包，供远端审核代码后测试 App。
- 在 /tmp clone 改 `app/build.gradle.kts`：versionCode 16→17、versionName 1.4.4→1.4.5（仅 2 行，产品代码零改动）。
- 提交 `1fca1c4` 并推送成功；`git ls-remote` 独立验证远端分支 HEAD = `1fca1c428d02...` ✅。推送用记忆中的 GCM helper 解法（GIT_CONFIG_SYSTEM 指向空 gitconfig 屏蔽 helper-selector），用完即删，token 未落盘。
- 重新构建 release（3m28s BUILD SUCCESSFUL）：SHA256 `6389840c...`，62,331,759 字节。
- 安装到手机：versionCode 17 / 1.4.5 ✅，lastUpdateTime 12:09:11，firstInstallTime 未变（数据保留），手机端 SHA256 与产物一致，启动 PID 911、0 崩溃。
- 归档 `E:\Claude_allow\matelink-apk-archive\matelink-1.4.5-build17-20260903-release.apk`；台账 `docs/ANDROID-RELEASE-LOG.md` 新增 1.4.5 条目（置顶）并与 1.4.4 做了对照表；`MEMORY.md` 当前线上版本更新为 1.4.5/17。
- 下一步：用户在远端审核 PR #4 代码，并在手机 1.4.5 上跑 7 项真机验证矩阵。

## 历史同步功能提交并推送远端（12:33 完成）
- 推进悬而未决的待办：本地↔云端历史同步功能提交到独立分支。（之前备份在 `/tmp/history-sync-backup/`，8 文件。）
- 基于干净 main（`11ba77d`，fetch 后可达）建扁平分支 `feat-history-sync-local-cloud`（规避本环境 checkout -b 缺陷）。
- **污染排查**：备份的 `main.go` 混入了上一 agent 的 readyz 诊断改动（`readinessTelemetryState`/`isFleetMode`）。对 8 个文件逐一 `diff -w -B`：4 个直接覆盖、2 个新增、main.go 手工只加 `case "history"` 路由（6 行），readyz 改动**未混入**（grep 验证 0 处）。
- 后端 `go build` 通过；历史导入相关 7 测试全 PASS（含按账户保留最新两自然日、按账户非按车、拒绝非法时间、须 POST）。
- 排除构建产物 `jourvolt-dev-api.exe`（19MB，记忆明确不应入库）。
- 提交 `318e242`（8 文件 +704 行），推送成功；`git ls-remote` 验证远端 HEAD = `318e242020f8...` ✅。PR 创建链接已由 GitHub 返回。
- **既有测试失败（pre-existing，非本次引入）**：`TestTaskDComposeSeparatesAPICAFromOfficialKeyAndUsesEphemeralConfig` 断言 docker-compose.yml 的 fleet-telemetry-config 须为 tmpfs volume。已在纯净 main(--depth 1) 复现同样失败 → 根因为记忆中「compose 修复尚未提交」（修复改动躺在工作区未入库）。本次改动未触及任何 compose/yml。
- 下一步：用户在 GitHub 创建 PR 审核；如需上服务器验证历史导入，须先补提交 compose tmpfs 修复（否则该测试在 CI 仍红）。
