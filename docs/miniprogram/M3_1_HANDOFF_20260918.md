# MateLink 微信小程序 M3.1 当前交接

日期：2026-09-18  
分支：`feature/wechat-miniprogram`  
远端 HEAD：`2797892d7a8c357ce81bdaefe8cdd7e9651a5415`  
版本：`0.2.2`

## 已完成

- Tesla 微信授权事务已按 API origin、canonical user、session generation、transaction proof 和 transaction id 隔离；claim 使用 single-flight，logout 先失效本地状态再用捕获凭据清理，重复消费后的微信 OAuth state 仍保留 `wechat` channel。
- 微信小程序业务路径不接受裸 ticket；错误授权不会泄露数据库内部错误，也不会错误回流 Android intent。
- 行程和充电页面共用 `history_controller`，已覆盖四页 66 条、失败重试、重复页、空末页、元数据矛盾和重复点击水位。
- 长期历史缓存按 schema/API origin/canonical account/stable vehicle/type 隔离；anonymous 读写 fail-closed，缓存写入失败不丢在线数据。
- `@babel/core` 已升级到 `7.29.7`；Taro `4.2.1` 的固定 peer 仍阻止安全升级 Webpack，未使用强制绕过。

## 最新验证

- 最终隔离目录实际 `npm ci --ignore-scripts`：1183 packages。
- TypeScript：PASS；Vitest：8 files / 54 tests PASS；微信构建：PASS。
- Go：0 failures、16 PostgreSQL skips；`go vet`、`go build`、`go mod verify` PASS。
- 业务产物扫描：`URLSearchParams=0`、`new URL=0`、动态环境变量=0、secret/token/private-key/password 标记=0。
- 当前工作区干净，本地 HEAD 与远端 HEAD 一致；未合并 `main`、未部署生产、未安装设备。

## 未完成门禁

- PostgreSQL 必须使用独立测试 DSN；缺失时由 `tools/miniprogram/verify_m3_1_pg_gate.ps1` 返回 `BLOCKED`，不能使用生产 DSN。
- `npm audit --omit=dev` 当前仍有 3 critical、8 moderate；Webpack/Taro 兼容升级需要上游可兼容版本，不能用 `--force` 或 `--legacy-peer-deps`。
- AppID、微信 request/web-view 域名、安卓/iOS 微信、Tesla OAuth、虚拟钥匙、首个 MQTT、真实行程/充电和换机历史恢复仍需人工/真实平台验证。

## 下一步

Jovi 可以开始注册小程序并准备平台域名。取得 AppID 和完成域名配置后，再使用本分支构建产物进行微信开发者工具、安卓微信和 iOS 微信验收；真实平台通过前不得把本地构建或 Mock/HTTP 状态称为发布就绪。
