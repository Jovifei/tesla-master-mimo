package com.matelink.ui.screens.auth

import androidx.compose.runtime.Composable

@Composable
fun DebugMockLoginEntry(onLoginSuccess: () -> Unit) {
    // The formal release source set intentionally has no Mock login UI.
}
